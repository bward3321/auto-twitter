name: Twitter Autoposter

on:
  schedule:
    # Weekly: Generate posts for ALL accounts every Sunday at 10am ET
    - cron: '0 15 * * 0'
    # Daily: Post approved content for ALL accounts at 8am ET
    - cron: '0 13 * * *'
  
  workflow_dispatch:
    inputs:
      command:
        description: 'Command to run'
        required: true
        default: 'post'
        type: choice
        options:
          - generate
          - post

jobs:
  generate:
    if: >
      (github.event_name == 'schedule' && github.event.schedule == '0 15 * * 0') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.command == 'generate')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - name: Generate weekly posts for all accounts
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GOOGLE_SHEETS_CREDS: ${{ secrets.GOOGLE_SHEETS_CREDS }}
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
          LEONARDO_API_KEY: ${{ secrets.LEONARDO_API_KEY }}
        run: python generate_all.py

  post:
    if: >
      (github.event_name == 'schedule' && github.event.schedule == '0 13 * * *') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.command == 'post')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -r requirements.txt
      - name: Post approved content for all accounts
        env:
          UPLOAD_POST_API_KEY: ${{ secrets.UPLOAD_POST_API_KEY }}
          UPLOAD_POST_USER: ${{ secrets.UPLOAD_POST_USER }}
          LEONARDO_API_KEY: ${{ secrets.LEONARDO_API_KEY }}
          GOOGLE_SHEETS_CREDS: ${{ secrets.GOOGLE_SHEETS_CREDS }}
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
        run: python post_all.py
